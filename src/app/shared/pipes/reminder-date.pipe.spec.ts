import { ReminderDatePipe } from './reminder-date.pipe';

describe('ReminderDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ReminderDatePipe();
    expect(pipe).toBeTruthy();
  });
});
